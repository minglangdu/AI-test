#pragma once

#include <iostream>
#include <vector>

#include "constants.h"

namespace AIH {
    struct Neuron {
        std::vector<double> weights;

    };
    struct Layer {

    };
    class Network {
        public:
            Network();
    };
}